import { Text, View } from "react-native";

export default function AdminUsers() {
    return (
        // CRUD DE USERS (já tem rota de listar todos.)
        <View>
            <Text>AdminUsers</Text>
        </View>
    );
}